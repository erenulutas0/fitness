# Normalise line endings so `dart format --set-exit-if-changed` behaves the same on Windows and CI.
* text=auto eol=lf
*.bat text eol=crlf
*.ps1 text eol=crlf
*.png binary
*.jpg binary
*.opus binary
*.task binary
*.tflite binary
*.glb binary
