## 0.1.0

- Platform interface, binary frame codec, fake engine (synthetic / fixture playback).
- Android: CameraX + MediaPipe Pose Landmarker (LIVE_STREAM, GPU delegate with CPU fallback), preview platform view.
- iOS: stub (NOT_SUPPORTED) — see TODO.md / docs/10 Prompt 8.
