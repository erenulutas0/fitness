import 'package:go_router/go_router.dart';

/// Onboarding routes (docs/06 §4.1). Empty until the onboarding screens
/// land; the router spreads this list so both sides compile meanwhile.
final List<RouteBase> onboardingRoutes = <RouteBase>[];
